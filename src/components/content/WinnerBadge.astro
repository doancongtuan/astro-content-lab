---
// src/components/content/WinnerBadge.astro
interface Props {
  winner: string
  itemA: string
  itemB: string
}

const { winner, itemA, itemB } = Astro.props

const normalize = (s: string) => s.trim().toLowerCase()
const w = normalize(winner)

type State = 'left' | 'right' | 'depends'

let state: State = 'depends'
if (w === normalize(itemA)) state = 'left'
else if (w === normalize(itemB)) state = 'right'

const stateStyles: Record<State, { class: string; label: string }> = {
  left: { class: 'bg-amber-50 dark:bg-amber-950 border-amber-200 dark:border-amber-800 text-amber-900 dark:text-amber-200', label: itemA },
  right: { class: 'bg-amber-50 dark:bg-amber-950 border-amber-200 dark:border-amber-800 text-amber-900 dark:text-amber-200', label: itemB },
  depends: { class: 'bg-slate-50 dark:bg-slate-800 border-slate-200 dark:border-slate-700 text-slate-700 dark:text-slate-300', label: 'Depends' },
}

const style = stateStyles[state]
---

<div class={`border rounded-xl p-4 mb-4 ${style.class}`}>
  🏆 <strong>Winner:</strong>
  <span class="capitalize"> {style.label}</span>
</div>